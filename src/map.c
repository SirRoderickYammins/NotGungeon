#include "raylib.h"
#include "map.h"
#include <stdio.h>

const float w = 1600.0f;
const float h = 1000.0f;

static void Map_TilePieceSize(Texture2D *tileMap){
    int tileMapPixelWidth_X = tileMap->width/9;
    int tileMapPixelHeight_Y = tileMap->height/8;
}


void Map_CreateRoom(Texture2D *tileMap){

    int tileMapPixelWidth_X = tileMap->width/9;
    int tileMapPixelHeight_Y = tileMap->height/8;

    

    // Get room size based off screen width

    


    

}